sudo: false
language: python
matrix:
  include:
    # Tests
    - python: 3.6.11
      env: TOXENV=py36-django30-wagtail210
cache:
  directories:
    - $HOME/.cache/pip
install:
  - pip install coveralls tox
script:
  - tox -e $TOXENV
after_success:
  - coveralls
notifications:
  email: false
